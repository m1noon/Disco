<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    >

    <ImageView
        android:id="@+id/a_collapsing_header_iv_header"
        android:layout_width="match_parent"
        android:layout_height="240dp"
        android:scaleType="centerCrop"
        android:background="@drawable/disco"
        />

    <android.support.v7.widget.RecyclerView
        android:id="@+id/a_collapsing_header_rv_list"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scrollbars="none"

        />

    <android.support.design.widget.FloatingActionButton
        android:id="@+id/a_collapsing_header_fab_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="210dp"
        android:layout_alignParentRight="true"
        app:fabSize="normal"
        />

    <android.support.v7.widget.Toolbar
        android:id="@+id/a_collapsing_header_tb_toolbar"
        android:layout_height="wrap_content"
        android:layout_width="match_parent"
        android:theme="@style/ThemeOverlay.AppCompat.ActionBar"
        android:minHeight="?attr/actionBarSize"
        android:background="@drawable/toolbar_bg_transition"
        app:titleTextColor="@android:color/white"
        />

</RelativeLayout>
